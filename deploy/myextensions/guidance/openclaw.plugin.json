{
  "id": "guidance",
  "name": "Global Guidance",
  "description": "Global guidance context engine that injects prompt additions from configured files.",
  "version": "2026.3.28",
  "kind": "context-engine",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "required": ["rootDir"],
    "properties": {
      "rootDir": {
        "type": "string",
        "minLength": 1
      },
      "files": {
        "type": "array",
        "items": {
          "type": "string"
        }
      }
    }
  },
  "uiHints": {
    "rootDir": {
      "label": "Guidance Root Directory",
      "help": "Base directory for resolving guidance file paths; absolute paths must also stay under this root"
    },
    "files": {
      "label": "Guidance Files",
      "help": "List of markdown files injected into system prompt addition"
    }
  }
}
